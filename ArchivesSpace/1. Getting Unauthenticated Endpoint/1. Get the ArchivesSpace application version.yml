info:
  name: 1. Get the ArchivesSpace application version
  type: http
  seq: 1

http:
  method: GET
  url: "{{ASPACE_BACKEND_URL}}/version"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: https://archivesspace.github.io/archivesspace/api/#get-the-archivesspace-application-version
